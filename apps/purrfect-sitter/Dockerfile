# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build purrfect-sitter`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t purrfect-sitter`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system purrfect-sitter && \
          adduser --system -G purrfect-sitter purrfect-sitter

COPY dist/apps/purrfect-sitter purrfect-sitter/
RUN chown -R purrfect-sitter:purrfect-sitter .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix purrfect-sitter --omit=dev -f install

CMD [ "node", "purrfect-sitter" ]
