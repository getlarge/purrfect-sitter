import { behavioralPatterns } from './behavioral-patterns.js';

describe('behavioralPatterns', () => {
  it('should work', () => {
    expect(behavioralPatterns()).toEqual('behavioral-patterns');
  });
});
