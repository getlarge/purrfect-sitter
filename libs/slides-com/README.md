# slides-com

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build slides-com` to build the library.

## Running unit tests

Run `nx test slides-com` to execute the unit tests via [Jest](https://jestjs.io).
